import type { MarketingAsset } from './types';

// Page media is added only after the immutable R2 object is uploaded and
// verified. An empty registry is intentional during infrastructure phases.
export const marketingAssets: readonly MarketingAsset[] = [];
