# Ideart Agent Skills

This package is the authoring root for Ideart-specific Agent Skills.

Add project-owned Skill directories and register prompt-only entries in
`catalog.json`. The release builder supports an empty catalog so Ideart can
ship the shared Skills infrastructure before adding its own Skills.
