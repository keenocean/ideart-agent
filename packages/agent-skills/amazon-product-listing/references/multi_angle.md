# Reconstructed Multi-Angle Template

This file is a reconstructed functional template inferred from `SKILL.md`. It is not an original creative platform side file.

Use with `model="nano-banana-2"`, `aspect_ratio="1:1"`, `resolution="2K"`, and `image_urls=["main:final"]`.

```text
Create an Amazon secondary multi-angle image for [BRAND] [PRODUCT_NAME], using the referenced main product image as the exact product source.

Show the product in a clean square studio layout with multiple consistent views: front, side, back, top/detail, and a scale or hero three-quarter angle when relevant. Arrange the views in a balanced grid or arc around one dominant hero view. Use a clean light neutral background with subtle shadows, no props unless included in the box.

Label each angle with short readable tags only if helpful: Front, Side, Back, Detail, Scale. Keep labels small but legible and visually secondary to the product.

Rules: every view must preserve identical packaging, color, label, logo, proportions, and variant. Do not invent unseen back-panel text; if the back view is unknown, use a neutral rear surface consistent with the visible product but avoid readable invented copy.
```
