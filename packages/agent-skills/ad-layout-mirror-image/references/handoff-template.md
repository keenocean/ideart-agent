# Ad Layout Mirror Image Handoff Template

Status: reconstructed from `SKILL.md`; not the original creative platform side file.

Use this as the director handoff for `output_type: image`, `aspect_ratio: 1:1`
unless the user asks to match a different reference ratio, and
`scene_type: live_action`.

## A. Product Reference

User product image / I2I anchor:

Packaging silhouette:

Must preserve exactly:

- wordmark spelling:
- decorative stripes / waves / illustrations:
- on-pack headline:
- feature bullets:
- weight / volume callout:
- URL or contact detail:
- cap / closure / shape:

## B. Layout

The reference image (`image-1`) is provided as a layout template only. Do not
include the reference's product container, logo, brand wordmark, claims, exact
copy, or brand colors unless the user explicitly owns them. Replicate only the
composition, spacing, hierarchy, lighting direction, and relative slot positions.

- top third:
- left of center:
- right of center:
- center / hero area:
- bottom band:
- background/surface:
- negative space:

## C. Text Overlays

Every word that appears in the static image must be listed here exactly. Omit
unverified claims. Do not copy quantitative claims from the reference.

- headline:
- subhead:
- proof badge:
- offer chip:
- CTA:
- logo/wordmark:
- legal/fine print:

## D. Critical Constraints

- Do not include the reference brand's product, product container, wordmark,
  colors, claims, or exact copy.
- Use only the user's clean product image as the product reference.
- Do not fabricate review counts, star ratings, prices, discounts, return
  windows, certifications, or clinical claims.
- All on-screen copy must render exactly as written, including punctuation and
  diacritics.
- If hands appear: fingers and wrist only, anatomically correct, five fingers, no
  extra digits.

## Render Direction

Photoreal live-action ad image. Match the reference layout geometry and visual
weight, but replace all content with the new product and brand-safe copy. Preserve
readability, clean spacing, and the original hierarchy. No copied third-party
wordmarks from the reference.
